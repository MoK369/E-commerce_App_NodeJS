export enum StorageTypesEnum {
  memory = 'memory',
  disk = 'disk',
}

export enum UploadFoldersEnum {
  brands = 'brands',
  categories = 'categories',
  products = 'products',
  coupons = 'coupons',
}
