export enum OrderStatusNamesEnum {
  pending = 'Pending',
  placed = 'Placed',
  onWay = 'On_Way',
  delivered = 'Delivered',
  canceled = 'Canceled',
}

export enum OrderStatusEnum {
  pending = 0,
  placed = 1,
  onWay = 2,
  delivered = 3,
  canceled = 4,
}

export enum PaymentTypesEnum {
  card = 'Card',
  cash = 'Cash',
}
